version: '3.4'

services:
  jekyll:
    image: jekyll/builder:latest
    command: "jekyll build"
    volumes:
      - $PWD:/srv/jekyll
  httpserver:
    image: nginx:latest
    ports:
      - 8080:80
    volumes:
      - $PWD/_site:/usr/share/nginx/html